@* @model Tenis3t.Models.Prestamo

@{
    ViewData["Title"] = "Marcar como Devuelto";
}

<div class="container mt-4">
    <h2>@ViewData["Title"]</h2>
    <h4>¿Confirmar devolución de este préstamo?</h4>
    <hr />
    
    <div class="row">
        <div class="col-md-6">
            <dl class="row">
                <dt class="col-sm-4">Producto:</dt>
                <dd class="col-sm-8">@Model.Inventario?.Nombre</dd>

                <dt class="col-sm-4">Tipo:</dt>
                <dd class="col-sm-8">@Model.TipoPrestamo</dd>

                <dt class="col-sm-4">@(Model.TipoPrestamo == "Realizado" ? "Prestado a:" : "Recibido de:")</dt>
                <dd class="col-sm-8">@Model.LocalPersona</dd>

                <dt class="col-sm-4">Cantidad:</dt>
                <dd class="col-sm-8">@Model.Cantidad</dd>

                <dt class="col-sm-4">Fecha Préstamo:</dt>
                <dd class="col-sm-8">@Model.FechaPrestamo.ToString("dd/MM/yyyy")</dd>

                <dt class="col-sm-4">Fecha Devolución:</dt>
                <dd class="col-sm-8">@Model.FechaDevolucionEstimada.ToString("dd/MM/yyyy")</dd>
            </dl>

            <form asp-action="Devolver">
                <input type="hidden" asp-for="Id" />
                <div class="form-group mt-4">
                    <button type="submit" class="btn btn-success">
                        <i class="bi bi-check-circle"></i> Confirmar Devolución
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-x-circle"></i> Cancelar
                    </a>
                </div>
            </form>
        </div>
    </div>
</div> *@